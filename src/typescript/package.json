{
  "name": "@a-world-of-trees/binary-search-tree",
  "version": "1.0.0",
  "description": "An implementation of a binary-search tree in Typescript.",
  "main": "index.ts",
  "scripts": {
    "postinstall": "npm run build",
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "test": "jest",
    "clean": "rm -rf dist/ && npx npkill",
    "audit": "npm audit && npx cdk synth | cfn_nag",
    "lint": "npx eslint ."
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.12",
    "@typescript-eslint/eslint-plugin": "^5.6.0",
    "@typescript-eslint/parser": "^5.6.0",
    "eslint": "^8.4.1",
    "jest": "^27.4.4",
    "ts-jest": "^27.1.1",
    "ts-node": "^10.4.0",
    "typescript": "~4.5.3"
  },
  "dependencies": {
    "npkill": "^0.8.2"
  }
}
